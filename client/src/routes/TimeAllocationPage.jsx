import React from 'react';
import TimeAllocationForm from '../components/TimeAllocationForm';

const TimeAllocationPage = ({user}) => {
  return <TimeAllocationForm user={user} />;
};

export default TimeAllocationPage;
